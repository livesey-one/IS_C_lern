#include <stdio.h>

int main() {

  // int b;
  // scanf("%d", &b);
  // for (short a = 0; a < b; a++) {
  //   printf("%d\n", a)
  // }
  int a = 1;
  while (a > 0) {
    scanf("%d", &a);
  };
  return 0;
}
